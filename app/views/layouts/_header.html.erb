<!-- Navigation panel -->
<nav class="main-nav dark transparent stick-fixed">

    <div class="full-wrapper relative clearfix">
        <!-- Logo ( * your text or image into link tag *) -->
        <div class="nav-logo-wrap local-scroll"> 
                <%= link_to image_tag("logo-white.png", alt: "Verify online logo"), root_url, turbolinks: false, class: "logo" %>
        </div>
      
        <div class="mobile-nav">     
        
            <i class="fa fa-bars"></i>
        </div>
        <!-- Main Menu -->
        <div class="inner-nav desktop-nav">
            <ul class="clearlist scroll-nav local-scroll">


                <% if params[:controller] =='landing_page' && params[:action] == 'index' %>
                    <li><%= link_to "Home", root_url, method: :get %></li>
                <% end %>

                <% if logged_in? %>
                    <li><%= link_to "Dashboard", apply_path, method: :get %></li>
                    <li><%= link_to "College", view_verifications_path, method: :get %></li>
                <% end %>

                <% if params[:controller] =='landing_page' && params[:action] == 'index' %>
                    <!-- <li><a href="#about">About</a></li> -->
                    <li><a href="#contact">Contact Us</a></li>         
                <% end %>
       
                <% if logged_in? %>
                    <li><%= link_to "Logout", logout_path, method: :delete %></li> 
                <% else %>
                    <% if params[:controller] != 'sessions' %>           
                        <li>
                            <%= link_to(login_path) do %>
                                <span class="btn btn-mod btn-circle btn-border-w">Login</span>
                            <%end%>
                        </li>      
                    <% end %>
                <% end %>
            </ul>
        </div>
    </div>
</nav>
<!-- End Navigation panel --> 